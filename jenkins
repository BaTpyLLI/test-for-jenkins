pipeline {
    agent any

    stages {
        stage('Hello') {
            when {changeset "*/test-for-jenkins/**"}
            steps {
                echo 'Hello World'
            }
        }
    }
}
